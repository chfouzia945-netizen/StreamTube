function esc(s){return String(s||"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
function card(v){return `<a class="video-card" href="watch.html?id=${encodeURIComponent(v.id)}">${v.thumbnail?`<img src="${v.thumbnail}">`:`<div class="thumb">▶</div>`}<div class="body"><h3>${esc(v.title)}</h3><div class="muted">${esc(v.creatorName)}</div><small class="muted">${(v.views||0).toLocaleString()} views • ${esc(v.category)}</small></div></a>`}
async function load(){let p=new URLSearchParams(location.search),q=p.get("q")||"",category=p.get("category")||"";search.value=q;let r=await fetch("/api/videos?"+new URLSearchParams({q,category}));let v=await r.json();heading.textContent=category?category+" videos":q?"Search results":"Recommended videos";count.textContent=v.length+" videos";grid.innerHTML=v.map(card).join("")||'<p class="muted">No videos found.</p>'}
function search(){location='/?q='+encodeURIComponent(document.getElementById("search").value)}document.getElementById("search").addEventListener("keydown",e=>{if(e.key==="Enter")search()});load();